[build]
  base = "FullStack"
  # CI= tells Netlify to ignore warnings and only stop for real errors
  command = "CI= npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200